# coding: utf-8
from typing import List
from pydantic import BaseModel, Field

{% for classwrapper in mod.classes %}class {{ classwrapper.class_name }}(BaseModel):
    {% for field in classwrapper.fields %}{{ field.name }}: {{ field.field_type }}
    {% endfor %}
{% endfor %}
